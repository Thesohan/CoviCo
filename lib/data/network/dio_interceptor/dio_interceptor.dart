//import 'package:dio/dio.dart';
//
//// Set up interceptors a dio instance
//void setUpInterceptors(Dio dio, [List<Interceptor> interceptors]) {
//  assert(dio != null);
//
//  if (interceptors != null && interceptors.isNotEmpty) {
//    dio.interceptors.addAll(interceptors);
//  }
//}
